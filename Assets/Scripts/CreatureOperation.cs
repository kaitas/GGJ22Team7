using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CreatureOperation
{
    [SerializeField] Transform _creatures_parent;       //_creaturesの親オブジェクト
    [SerializeField] Creature _creatures;


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
